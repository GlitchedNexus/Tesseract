pub struct LogisticRegressions {}

impl Default for LogisticRegressions {
    fn default() -> Self {
        Self {}
    }
}

impl LogisticRegressions {
    pub fn new() {
        Self::default();
    }

    pub fn fit() {}

    pub fn predict() {}
}
